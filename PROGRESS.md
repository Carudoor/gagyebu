# 작업이 멈췄던 이유

Claude Code(이 어시스턴트)는 Discord 메시지 하나당 "한 턴"으로 일함 — 요청받은
작업 단위를 끝내면 결과를 보고하고 그 턴을 마침. 중간에 크래시하거나 막힌 게
아니라, "홈 화면 먼저 만들어서 보여드리고, 확인되면 나머지도 이어서 진행할게요"
라고 미리 말한 대로 **1단계(디자인 토큰 + 하단 탭바 + 홈 화면)까지만 끝내고
의도적으로 멈춰서 보고한 것**임. 사람이 옆에서 계속 "다음 할 일"을 이어서
시키지 않으면, 다음 메시지가 올 때까지는 스스로 다음 단계로 넘어가지 않음.

즉, 별도로 고쳐야 할 버그는 없고 — "계속 진행해줘" 같은 한마디가 다음 턴을
시작하는 신호임. 여러 단계짜리 작업을 이어서 쭉 하고 싶으면 그렇게 말해주면
됨(예: "확인 필요 없이 끝까지 진행해줘").

## 모바일 리디자인 완료 (2026-09-23)

"계속 진행해줘" 요청을 받고 나머지 전부 끝냄:

- 기록 화면(거래 입력/목록, 카테고리별 통계, 그래프)과 설정 화면(정기 구독,
  백업)을 전부 새 디자인으로 다시 만듦 — 바텀 시트 입력 폼, 카드 리스트,
  플로팅 + 버튼 등
- 이제 CoreUI(`@coreui/react`, `@coreui/coreui`)를 쓰는 화면이 하나도 없어서
  **완전히 제거함** — Tailwind `preflight` 다시 켜고 `tw:` 접두사 hack도 없앰.
  CSS 번들 315KB → 16KB로 줄어듦
- 아이콘(`@coreui/icons-react`)은 계속 쓰는데, 이 패키지 자체 CSS를 안전하게
  못 불러와서 필요한 크기 클래스만 `index.css`에 옮겨 적음 (제거 직후
  아이콘이 버튼 전체를 채울 만큼 커지는 버그 있었는데 이걸로 고침)
- 390px(아이폰) 기준으로 전체 화면 라이트/다크 모드, 바텀시트 열기/입력/저장,
  오프라인 재확인까지 헤드리스 브라우저로 검증함

이 파일은 앞으로 세션이 오래 끊겼다 다시 시작될 때 "왜 멈췄었는지" 헷갈리지
않게 하려고 만든 것. 아키텍처/기술적 결정 기록은 계속 `CLAUDE.md`에 남김.
